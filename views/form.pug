extends layout

block content
  div.text-center
    form(class="form-signin" method="POST" action="/new")
      div(class="form-group")
        label(for="author" class="sr-only") Email address
        input(type="text" name="author" id="author" class="form-control" placeholder="Author namee" required autofocus)
      div(class="form-group")
        label(for="text" class="sr-only") Password
        input(type="text" name="text" id="text" class="form-control" placeholder="MESSAGEE" require)
      button(class="submit" type="submit") SUBMITTT